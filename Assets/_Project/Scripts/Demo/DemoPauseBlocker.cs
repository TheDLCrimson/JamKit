using JamKit;

// Demo pause menu blocker for Demo_Feel. Plain Assembly-CSharp, no namespace (jam-code convention).
// Uses the UIBlockerBase defaults (pauseGameplay + respondToEscape), so opening it pauses the game and
// pressing the UI Cancel action closes it.
public class DemoPauseBlocker : UIBlockerBase
{
}
